<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authFragment">

    <fragment
        android:id="@+id/authFragment"
        android:name="github.owlmail.auth.AuthFragment"
        android:label="AuthFragment" >
        <action
            android:id="@+id/action_authFragment_to_mailFragment"
            app:destination="@id/mailFragment" />
    </fragment>
    <fragment
        android:id="@+id/mailFragment"
        android:name="github.owlmail.mail.MailFragment"
        android:label="MailFragment" />
</navigation>